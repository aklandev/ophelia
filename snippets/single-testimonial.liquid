{% comment %}
  Renders a Single Testimonial:

  Accepts:
  - content: {String} Content / Main text
  - author: {String} Name of the person
  - grain: {Image Object} Background Image of the section | Optional
  - avatar: {Image Object} Image of the person | Optional

  Usage:
  {% render 'single-testimonial' content: block.settings.review_content, author: review_author %}
{% endcomment %}

<div
  class="testimonial mx-n7.7 mx-n1.5--desk mb-1.5 mb-0--desk overflow-hidden"
  {% if grain != blank %}
    style="background-image: url('{{ grain | image_url }}')"
  {% endif %}
>
  <div class="testimonial__content-wrapper mx-auto">
    <div class="h2 text-2.3 text-3.5--desk lh-140 lh-133--desk font-light">{{ content }}</div>
    <div class="mt-1.5 mt-2.5--desk d-flex align-items-center lh-150">
      {% if avatar != blank %}
        <div class="testimonial__avatar mr-2 rounded-circle overflow-hidden">
          <div class="media media--square media--lazy-animation">
            <img
              src="{{ avatar | image_url: width: 100, height: 100 }}"
              class="z-behind"
              width="100"
              height="100"
              loading="lazy"
              alt="{{ avatar.alt }}"
            >
          </div>
        </div>
      {% endif %}
      <div class="rte text-1.54 text-1.85--desk">
        <p>{{ author }}</p>
      </div>
    </div>
  </div>
</div>
